{
  "name": "Inn rumours create quest offers",
  "goal": "Once the Inn finishes producing a rumour, a quest offer should appear on the board.",
  "seed": 42,
  "max_ticks": 0,
  "commands": [
    { "cmd": "reset_world", "seed": 42 },
    { "cmd": "place_building", "type": "tavern", "x": 0, "z": 0 },
    { "cmd": "step_ticks", "n": 840 }
  ],
  "assertions": [
    { "assert": "building_exists", "type": "tavern" },
    { "assert": "quest_count_gte", "value": 1 },
    { "assert": "event_type_seen", "value": "building_output_completed" }
  ]
}
