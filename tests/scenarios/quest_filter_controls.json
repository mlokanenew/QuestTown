{
  "name": "Quest filters control available offers",
  "goal": "Disable every quest template except Clear Rats from the Cellar and verify only that offer type remains available.",
  "seed": 42,
  "llm_bootstrap_commands": true,
  "max_ticks": 360,
  "commands": [
    {"cmd": "set_quest_enabled", "id": "gather_rare_herbs", "enabled": false},
    {"cmd": "set_quest_enabled", "id": "investigate_strange_lights", "enabled": false},
    {"cmd": "place_building", "type": "tavern", "x": 0, "z": 0}
  ],
  "assertions": [
    {"assert": "quest_count_gte", "value": 1},
    {"assert": "quest_templates_only", "value": ["clear_rats_cellar"]}
  ]
}
