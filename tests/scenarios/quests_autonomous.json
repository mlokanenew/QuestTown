{
  "name": "Accepted quests start running",
  "goal": "Place a tavern, generate a quest from Inn rumours, and explicitly accept it to launch a party.",
  "seed": 42,
  "max_ticks": 1800,
  "commands": [
    {"cmd": "place_building", "type": "tavern", "x": 0, "z": 0},
    {"cmd": "set_building_output_mode", "type": "tavern"},
    {"cmd": "step_ticks", "n": 1600},
    {"cmd": "accept_quest"}
  ],
  "assertions": [
    {"assert": "hero_count_gte", "value": 1},
    {"assert": "event_type_seen", "value": "quest_accepted"},
    {"assert": "event_type_seen", "value": "hero_started_quest"}
  ]
}
