{
  "name": "Fresh Inn without rumours exposes no quests",
  "goal": "A newly built Inn should not generate quests until it has finished producing at least one rumour.",
  "seed": 42,
  "max_ticks": 0,
  "commands": [
    { "cmd": "reset_world", "seed": 42 },
    { "cmd": "place_building", "type": "tavern", "x": 0, "z": 0 },
    { "cmd": "step_ticks", "n": 600 }
  ],
  "assertions": [
    { "assert": "building_exists", "type": "tavern" },
    { "assert": "building_output_stock_gte", "type": "tavern", "value": 0 },
    { "assert": "quest_count_eq", "value": 0 }
  ]
}
